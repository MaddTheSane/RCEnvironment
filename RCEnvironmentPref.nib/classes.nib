{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = NSPreferencePane; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "_firstKeyView" = id; 
                "_initialKeyView" = id; 
                "_lastKeyView" = id; 
                "_window" = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {loadBackup = id; revert = id; save = id; showWebsite = id; }; 
            CLASS = RCEnvironmentPref; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                aboutField = NSTextView; 
                addButton = NSButton; 
                backupButton = NSButton; 
                editButton = NSButton; 
                inspectButton = NSButton; 
                inspectButtons = NSMatrix; 
                inspectTitle = NSTextField; 
                keyValueDataSource = RCKeyValueDataSource; 
                nextLoginField = NSTextField; 
                removeButton = NSButton; 
                revertButton = NSButtonCell; 
                saveButton = NSButtonCell; 
                tabView = NSTabView; 
                tableView = NSTableView; 
                valueColumn = NSTableColumn; 
                variableColumn = NSTableColumn; 
                versionField = NSTextField; 
            }; 
            SUPERCLASS = NSPreferencePane; 
        }, 
        {
            ACTIONS = {addItem = id; editItem = id; endEditItem = id; removeItems = id; }; 
            CLASS = RCKeyValueDataSource; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                delegate = id; 
                editKey = NSTextField; 
                editValue = NSText; 
                inspectWindow = NSWindow; 
                tableView = NSTableView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = RCKeyValueDataSourceDelegate; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}